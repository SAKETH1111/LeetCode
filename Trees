

In LinkedList if you want add element to the list you use
-> List.add(1);
If you want to add it in thr front you must use addFirst()
-> List.addFirst(2);


poll(): removes and retrieves the head of the queue. 
q. peek(): does not remove but only retrieves/returns the head of the queue
